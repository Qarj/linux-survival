const lastSyncTime = 1659181584;
// show last sync time in human readable format
const lastSyncTimeHuman = new Date(lastSyncTime * 1000).toLocaleString();
console.log(`Last sync: ${lastSyncTimeHuman}`);
